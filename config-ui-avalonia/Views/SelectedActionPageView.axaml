<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:MyKeymap.Settings.ViewModels"
             xmlns:views="using:MyKeymap.Settings.Views"
             xmlns:controls="using:MyKeymap.Settings.Controls"
             x:Class="MyKeymap.Settings.Views.SelectedActionPageView"
             x:DataType="vm:SelectedActionPageViewModel">

  <!--
    评审 D1 定案 (2026-09-06): 方案 D 下 entry.options 三开关 (复制到剪贴板/清空选区/执行前确认)
    暂缓消费, 本版 UI 不呈现 —— SelectedAction.ahk 引擎未定义其菜单语义, 生成端也不把
    options 写进数据数组。模型字段 (SelectedEntry.Options / EntryRowVm 三属性) 保留不删,
    兼容存量数据往返, 前向兼容恢复。
  -->

  <UserControl.Styles>
    <!-- 映射行卡: 悬停提亮边框 / 测试命中高亮 (蓝底) -->
    <Style Selector="Border.row-card:pointerover">
      <Setter Property="BorderBrush" Value="#9DB8EC" />
    </Style>
    <Style Selector="Border.row-card.matched">
      <Setter Property="Background" Value="#E8F0FE" />
      <Setter Property="BorderBrush" Value="#4169E1" />
    </Style>
    <!-- 行为胶囊 (chip): 彩色描边 + 同色文字, 徽章色由 VM 按 BaseActionOf 推导 -->
    <Style Selector="Border.chip">
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="10" />
      <Setter Property="Padding" Value="8,2" />
      <Setter Property="Margin" Value="0,0,6,6" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <!-- 小型图标按钮 (↑ ↓ ✕ ▶) -->
    <Style Selector="Button.icon-btn">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="6,2" />
      <Setter Property="MinWidth" Value="28" />
      <Setter Property="FontSize" Value="13" />
    </Style>
  </UserControl.Styles>

  <Grid Margin="20" RowDefinitions="Auto,*">

    <!-- ===================== 顶部: 标题 + 管理行为… + 添加映射 ===================== -->
    <DockPanel Grid.Row="0" Margin="0,0,0,12">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
        <Button Click="OnManageBehaviors" Background="Transparent" BorderThickness="1"
                BorderBrush="#E3E6EC" CornerRadius="8" Padding="12,7" FontSize="13"
                Content="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1083}" />
        <Button Command="{Binding OpenAddPanelCommand}"
                Background="#4169E1" Foreground="White" CornerRadius="8" Padding="14,7">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <TextBlock Text="＋" />
            <TextBlock Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1105}" />
          </StackPanel>
        </Button>
      </StackPanel>
      <TextBlock FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"
                 Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=914}" />
    </DockPanel>

    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled">
      <StackPanel MaxWidth="860" HorizontalAlignment="Left" Spacing="14">

        <!-- 说明条 (单键分发语义, 复用 960) -->
        <Border Background="#E8F0FE" CornerRadius="8" Padding="12,9">
          <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#37474F"
                     Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=960}" />
        </Border>

        <!-- 主快捷键 + 启用开关 -->
        <Border Background="White" BorderBrush="#E3E6EC" BorderThickness="1"
                CornerRadius="10" Padding="16">
          <StackPanel Spacing="10">
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock FontWeight="SemiBold" VerticalAlignment="Center"
                         Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1063}" />
              <ToggleSwitch Grid.Column="1" IsChecked="{Binding Enable}"
                            OnContent="{Binding EnableLabel}" OffContent="{Binding EnableLabel}"
                            MinWidth="10" />
            </Grid>
            <controls:HotkeyCapture Hotkey="{Binding Hotkey, Mode=TwoWay}"
                                    UsedHotkeys="{Binding UsedHotkeys}" />
            <TextBlock IsVisible="{Binding NoHotkeyWarning}" FontSize="12" Foreground="#B26A00"
                       TextWrapping="Wrap"
                       Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=976}" />
            <Border IsVisible="{Binding HotkeyPendingSave}" Background="#FFF8E1"
                    CornerRadius="6" Padding="10,6">
              <TextBlock FontSize="12" Foreground="#B26A00" TextWrapping="Wrap"
                         Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1077}" />
            </Border>
          </StackPanel>
        </Border>

        <!-- 匹配优先级说明 (组内行序 = 优先级) -->
        <TextBlock FontSize="12" Foreground="#8A8F98" TextWrapping="Wrap"
                   Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=978}" />

        <!-- 空状态 -->
        <StackPanel IsVisible="{Binding !HasAnyMappings}" Spacing="8" Margin="0,20">
          <TextBlock Text="👆" FontSize="40" HorizontalAlignment="Center" />
          <TextBlock FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"
                     Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1112}" />
          <TextBlock FontSize="12" Foreground="#8A8F98" HorizontalAlignment="Center"
                     Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1113}" />
        </StackPanel>

        <!-- ===================== 分区一: 文本特征 ===================== -->
        <StackPanel IsVisible="{Binding TextMappings.Count}" Spacing="8">
          <TextBlock FontWeight="SemiBold" FontSize="15"
                     Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1032}" />
          <ItemsControl ItemsSource="{Binding TextMappings}">
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="vm:MappingRowVm">

                <Border Classes="row-card" Classes.matched="{Binding IsMatched}"
                        Background="White" BorderBrush="#E3E6EC" BorderThickness="1"
                        CornerRadius="10" Padding="12" Margin="0,0,0,8"
                        Tapped="OnRowHeaderTapped">
                  <StackPanel Spacing="8">
                    <!-- 行头: 类型徽章 + 条件值编辑 + 行操作 -->
                    <Grid ColumnDefinitions="Auto,*,Auto">
                      <Border Background="{Binding TypeBadgeColorHex}" CornerRadius="6"
                              Padding="8,3" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TypeBadgeText}" Foreground="White" FontSize="12" />
                      </Border>

                      <StackPanel Grid.Column="1" Margin="10,0" Spacing="6" VerticalAlignment="Center">
                        <!-- textType: 文本特征下拉 (四选一) -->
                        <ComboBox IsVisible="{Binding IsTextType}" MinWidth="220"
                                  ItemsSource="{Binding TextTypeOptions}"
                                  SelectedItem="{Binding TextTypeSelected}" />
                        <!-- fileExt: 条件值 + 分组快捷填入 -->
                        <StackPanel IsVisible="{Binding !IsTextType}" Spacing="6">
                          <TextBox Text="{Binding MatchValueDisplay}"
                                   Watermark="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1034}" />
                          <StackPanel IsVisible="{Binding ShowFileGroupFill}" Orientation="Horizontal" Spacing="6">
                            <TextBlock FontSize="12" Foreground="#8A8F98" VerticalAlignment="Center"
                                       Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1006}" />
                            <ComboBox MinWidth="160"
                                      ItemsSource="{Binding FileGroupOptions}"
                                      SelectedItem="{Binding FileGroupSelected}" />
                          </StackPanel>
                        </StackPanel>
                        <TextBlock Text="{Binding MatchHint}" FontSize="11" Foreground="#8A8F98"
                                   TextWrapping="Wrap" />
                      </StackPanel>

                      <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Top">
                        <Button Classes="icon-btn" Command="{Binding TestRowCommand}" Content="▶"
                                ToolTip.Tip="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=990}" />
                        <Button Classes="icon-btn" Command="{Binding MoveUpCommand}" IsEnabled="{Binding CanMoveUp}" Content="↑" />
                        <Button Classes="icon-btn" Command="{Binding MoveDownCommand}" IsEnabled="{Binding CanMoveDown}" Content="↓" />
                        <Button Classes="icon-btn" Command="{Binding AskRemoveCommand}" Content="✕" Foreground="#D32F2F" />
                      </StackPanel>
                    </Grid>

                    <!-- chips 键位表 (序号即菜单数字键) -->
                    <ItemsControl ItemsSource="{Binding Chips}">
                      <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                          <WrapPanel />
                        </ItemsPanelTemplate>
                      </ItemsControl.ItemsPanel>
                      <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:EntryChipVm">
                          <Border Classes="chip" BorderBrush="{Binding ColorHex}">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                              <TextBlock Text="{Binding Index}" Foreground="{Binding ColorHex}"
                                         FontSize="12" FontWeight="SemiBold" />
                              <TextBlock Text="{Binding Label}" Foreground="{Binding ColorHex}" FontSize="12" />
                            </StackPanel>
                          </Border>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- 行内手风琴编辑器 (同屏只开一个, 由页面 ExpandedRow 仲裁) -->
                    <StackPanel IsVisible="{Binding IsExpanded}" Spacing="8" Margin="0,4,0,0">
                      <ItemsControl ItemsSource="{Binding Editors}">
                        <ItemsControl.ItemTemplate>
                          <DataTemplate x:DataType="vm:EntryRowVm">
                            <Border Background="#FAFBFC" BorderBrush="#ECEFF3" BorderThickness="1"
                                    CornerRadius="8" Padding="10" Margin="0,0,0,6">
                              <StackPanel Spacing="8">
                                <Grid ColumnDefinitions="Auto,*,Auto">
                                  <Border Background="#E8F0FE" CornerRadius="6" Padding="7,2"
                                          VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Index}" Foreground="#4169E1"
                                               FontWeight="SemiBold" FontSize="13" />
                                  </Border>
                                  <ComboBox Grid.Column="1" Margin="10,0" HorizontalAlignment="Stretch"
                                            ItemsSource="{Binding BehaviorOptions}"
                                            SelectedItem="{Binding BehaviorSelected}" />
                                  <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2">
                                    <Button Classes="icon-btn" Command="{Binding MoveUpCommand}"
                                            IsEnabled="{Binding CanMoveUp}" Content="↑" />
                                    <Button Classes="icon-btn" Command="{Binding MoveDownCommand}"
                                            IsEnabled="{Binding CanMoveDown}" Content="↓" />
                                    <Button Classes="icon-btn" Command="{Binding RemoveCommand}"
                                            IsEnabled="{Binding CanRemove}" Content="✕" Foreground="#D32F2F"
                                            ToolTip.Tip="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1108}" />
                                  </StackPanel>
                                </Grid>
                                <TextBlock Text="{Binding BehaviorHint}" FontSize="11" Foreground="#8A8F98"
                                           TextWrapping="Wrap" />
                                <!-- 命令模板 -->
                                <StackPanel IsVisible="{Binding ShowTemplate}" Spacing="4">
                                  <TextBlock FontSize="11" Foreground="#8A8F98"
                                             Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1012}" />
                                  <TextBox Text="{Binding ActionValue}" TextWrapping="Wrap" AcceptsReturn="False" />
                                </StackPanel>
                                <TextBlock IsVisible="{Binding IsNoValue}" FontSize="11" Foreground="#2E7D32"
                                           TextWrapping="Wrap" Text="{Binding TemplateHint}" />
                                <!-- 工作目录 -->
                                <TextBox Text="{Binding WorkingDir}"
                                         Watermark="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1015}" />
                                <!-- 评审 D1: Options 三开关 (1016/1017/1018) 本版不渲染, 模型字段保留前向兼容 (见文件头注记) -->
                              </StackPanel>
                            </Border>
                          </DataTemplate>
                        </ItemsControl.ItemTemplate>
                      </ItemsControl>

                      <!-- 添加行为 (达 9 禁用 + 提示) -->
                      <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Command="{Binding AddEntryCommand}" IsEnabled="{Binding CanAddEntry}"
                                Background="Transparent" BorderThickness="1" BorderBrush="#4169E1"
                                Foreground="#4169E1" CornerRadius="8" Padding="12,5" FontSize="12"
                                Content="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1106}" />
                        <TextBlock IsVisible="{Binding !CanAddEntry}" FontSize="12" Foreground="#B26A00"
                                   VerticalAlignment="Center"
                                   Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1107}" />
                      </StackPanel>
                    </StackPanel>
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>

        <!-- ===================== 分区二: 文件后缀 ===================== -->
        <StackPanel IsVisible="{Binding FileMappings.Count}" Spacing="8">
          <TextBlock FontWeight="SemiBold" FontSize="15"
                     Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1031}" />
          <ItemsControl ItemsSource="{Binding FileMappings}">
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="vm:MappingRowVm">

                <Border Classes="row-card" Classes.matched="{Binding IsMatched}"
                        Background="White" BorderBrush="#E3E6EC" BorderThickness="1"
                        CornerRadius="10" Padding="12" Margin="0,0,0,8"
                        Tapped="OnRowHeaderTapped">
                  <StackPanel Spacing="8">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                      <Border Background="{Binding TypeBadgeColorHex}" CornerRadius="6"
                              Padding="8,3" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TypeBadgeText}" Foreground="White" FontSize="12" />
                      </Border>

                      <StackPanel Grid.Column="1" Margin="10,0" Spacing="6" VerticalAlignment="Center">
                        <StackPanel IsVisible="{Binding !IsTextType}" Spacing="6">
                          <TextBox Text="{Binding MatchValueDisplay}"
                                   Watermark="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1034}" />
                          <StackPanel IsVisible="{Binding ShowFileGroupFill}" Orientation="Horizontal" Spacing="6">
                            <TextBlock FontSize="12" Foreground="#8A8F98" VerticalAlignment="Center"
                                       Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1006}" />
                            <ComboBox MinWidth="160"
                                      ItemsSource="{Binding FileGroupOptions}"
                                      SelectedItem="{Binding FileGroupSelected}" />
                          </StackPanel>
                        </StackPanel>
                        <ComboBox IsVisible="{Binding IsTextType}" MinWidth="220"
                                  ItemsSource="{Binding TextTypeOptions}"
                                  SelectedItem="{Binding TextTypeSelected}" />
                        <TextBlock Text="{Binding MatchHint}" FontSize="11" Foreground="#8A8F98"
                                   TextWrapping="Wrap" />
                      </StackPanel>

                      <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Top">
                        <Button Classes="icon-btn" Command="{Binding TestRowCommand}" Content="▶"
                                ToolTip.Tip="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=990}" />
                        <Button Classes="icon-btn" Command="{Binding MoveUpCommand}" IsEnabled="{Binding CanMoveUp}" Content="↑" />
                        <Button Classes="icon-btn" Command="{Binding MoveDownCommand}" IsEnabled="{Binding CanMoveDown}" Content="↓" />
                        <Button Classes="icon-btn" Command="{Binding AskRemoveCommand}" Content="✕" Foreground="#D32F2F" />
                      </StackPanel>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding Chips}">
                      <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                          <WrapPanel />
                        </ItemsPanelTemplate>
                      </ItemsControl.ItemsPanel>
                      <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:EntryChipVm">
                          <Border Classes="chip" BorderBrush="{Binding ColorHex}">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                              <TextBlock Text="{Binding Index}" Foreground="{Binding ColorHex}"
                                         FontSize="12" FontWeight="SemiBold" />
                              <TextBlock Text="{Binding Label}" Foreground="{Binding ColorHex}" FontSize="12" />
                            </StackPanel>
                          </Border>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <StackPanel IsVisible="{Binding IsExpanded}" Spacing="8" Margin="0,4,0,0">
                      <ItemsControl ItemsSource="{Binding Editors}">
                        <ItemsControl.ItemTemplate>
                          <DataTemplate x:DataType="vm:EntryRowVm">
                            <Border Background="#FAFBFC" BorderBrush="#ECEFF3" BorderThickness="1"
                                    CornerRadius="8" Padding="10" Margin="0,0,0,6">
                              <StackPanel Spacing="8">
                                <Grid ColumnDefinitions="Auto,*,Auto">
                                  <Border Background="#E8F0FE" CornerRadius="6" Padding="7,2"
                                          VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Index}" Foreground="#4169E1"
                                               FontWeight="SemiBold" FontSize="13" />
                                  </Border>
                                  <ComboBox Grid.Column="1" Margin="10,0" HorizontalAlignment="Stretch"
                                            ItemsSource="{Binding BehaviorOptions}"
                                            SelectedItem="{Binding BehaviorSelected}" />
                                  <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2">
                                    <Button Classes="icon-btn" Command="{Binding MoveUpCommand}"
                                            IsEnabled="{Binding CanMoveUp}" Content="↑" />
                                    <Button Classes="icon-btn" Command="{Binding MoveDownCommand}"
                                            IsEnabled="{Binding CanMoveDown}" Content="↓" />
                                    <Button Classes="icon-btn" Command="{Binding RemoveCommand}"
                                            IsEnabled="{Binding CanRemove}" Content="✕" Foreground="#D32F2F"
                                            ToolTip.Tip="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1108}" />
                                  </StackPanel>
                                </Grid>
                                <TextBlock Text="{Binding BehaviorHint}" FontSize="11" Foreground="#8A8F98"
                                           TextWrapping="Wrap" />
                                <StackPanel IsVisible="{Binding ShowTemplate}" Spacing="4">
                                  <TextBlock FontSize="11" Foreground="#8A8F98"
                                             Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1012}" />
                                  <TextBox Text="{Binding ActionValue}" TextWrapping="Wrap" AcceptsReturn="False" />
                                </StackPanel>
                                <TextBlock IsVisible="{Binding IsNoValue}" FontSize="11" Foreground="#2E7D32"
                                           TextWrapping="Wrap" Text="{Binding TemplateHint}" />
                                <TextBox Text="{Binding WorkingDir}"
                                         Watermark="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1015}" />
                                <!-- 评审 D1: Options 三开关 (1016/1017/1018) 本版不渲染, 模型字段保留前向兼容 (见文件头注记) -->
                              </StackPanel>
                            </Border>
                          </DataTemplate>
                        </ItemsControl.ItemTemplate>
                      </ItemsControl>

                      <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Command="{Binding AddEntryCommand}" IsEnabled="{Binding CanAddEntry}"
                                Background="Transparent" BorderThickness="1" BorderBrush="#4169E1"
                                Foreground="#4169E1" CornerRadius="8" Padding="12,5" FontSize="12"
                                Content="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1106}" />
                        <TextBlock IsVisible="{Binding !CanAddEntry}" FontSize="12" Foreground="#B26A00"
                                   VerticalAlignment="Center"
                                   Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1107}" />
                      </StackPanel>
                    </StackPanel>
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>

        <!-- ===================== 底部紧凑模拟测试条 ===================== -->
        <Border Background="White" BorderBrush="#E3E6EC" BorderThickness="1"
                CornerRadius="10" Padding="16">
          <StackPanel Spacing="8">
            <TextBlock FontWeight="SemiBold"
                       Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=987}" />
            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
              <TextBox Text="{Binding TestContent}" MinWidth="120"
                       Watermark="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=988}" />
              <CheckBox Grid.Column="1" IsChecked="{Binding TestIsFile}" VerticalAlignment="Center"
                        Content="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=989}" />
              <Button Grid.Column="2" Command="{Binding RunTestCommand}" Background="#4169E1"
                      Foreground="White" CornerRadius="8" Padding="16,7" VerticalAlignment="Center"
                      IsEnabled="{Binding !Testing}"
                      Content="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=990}" />
            </Grid>
            <TextBlock IsVisible="{Binding TestError}" Foreground="#D32F2F" FontSize="12"
                       TextWrapping="Wrap" Text="{Binding TestError}" />
            <StackPanel IsVisible="{Binding HasResult}" Spacing="6">
              <!-- 未命中 -->
              <TextBlock IsVisible="{Binding !ResultMatched}" FontSize="12" Foreground="#8A8F98"
                         Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=993}" />
              <!-- 命中: 类型 + 值 + 菜单键位 + 预览 -->
              <StackPanel IsVisible="{Binding ResultMatched}" Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <Border Background="#E8F0FE" CornerRadius="6" Padding="8,3">
                    <TextBlock Text="{Binding MatchedTypeText}" Foreground="#4169E1" FontSize="12" />
                  </Border>
                  <TextBlock Text="{Binding MatchedValueText}" FontSize="13" FontWeight="SemiBold"
                             VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <TextBlock FontSize="12" Foreground="#8A8F98" VerticalAlignment="Center"
                             Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1110}" />
                  <ItemsControl ItemsSource="{Binding MenuKeys}">
                    <ItemsControl.ItemsPanel>
                      <ItemsPanelTemplate>
                        <WrapPanel />
                      </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                      <DataTemplate x:DataType="vm:MenuKeyVm">
                        <Border Classes="chip" BorderBrush="{Binding ColorHex}">
                          <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock Text="{Binding Key}" Foreground="{Binding ColorHex}"
                                       FontSize="12" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Name}" Foreground="{Binding ColorHex}" FontSize="12" />
                          </StackPanel>
                        </Border>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <TextBlock FontSize="12" Foreground="#8A8F98" VerticalAlignment="Center"
                             Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=996}" />
                  <TextBlock Text="{Binding PreviewText}" FontSize="12" TextWrapping="Wrap"
                             VerticalAlignment="Center" />
                </StackPanel>
              </StackPanel>
            </StackPanel>
          </StackPanel>
        </Border>

      </StackPanel>
    </ScrollViewer>

    <!-- ===================== 添加映射弹窗 (页内 overlay, 非独立窗口) ===================== -->
    <Border Grid.RowSpan="2" IsVisible="{Binding IsAddPanelOpen}" Background="#88000000">
      <ContentControl Content="{Binding AddPanel}" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ContentControl.DataTemplates>
          <DataTemplate x:DataType="vm:AddMappingVm">
            <Border MaxWidth="560" MinWidth="440" HorizontalAlignment="Left"
                    Background="White" CornerRadius="12" Padding="20" Margin="20"
                    BoxShadow="0 8 24 rgba(0,0,0,0.25)">
              <StackPanel Spacing="12">
                <!-- 标题 + 说明 -->
                <TextBlock FontSize="17" FontWeight="SemiBold"
                           Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1105}" />
                <TextBlock FontSize="12" Foreground="#5F6368" TextWrapping="Wrap" Text="{Binding Hint}" />

                <!-- 步骤 1: 类型 (文件后缀 + 文本特征四选一) -->
                <StackPanel Spacing="4">
                  <TextBlock FontSize="12" Foreground="#8A8F98"
                             Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1004}" />
                  <ComboBox MinWidth="220" ItemsSource="{Binding TypeOptions}"
                            SelectedItem="{Binding TypeSelected}" />
                </StackPanel>

                <!-- 步骤 2: 条件值 (仅文件后缀) -->
                <StackPanel Spacing="4" IsVisible="{Binding IsFileExt}">
                  <TextBlock FontSize="12" Foreground="#8A8F98"
                             Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1005}" />
                  <TextBox Text="{Binding MatchValue}"
                           Watermark="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1034}" />
                  <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock FontSize="12" Foreground="#8A8F98" VerticalAlignment="Center"
                               Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1006}" />
                    <ComboBox MinWidth="160" ItemsSource="{Binding FileGroupOptions}"
                              SelectedItem="{Binding FileGroupSelected}" />
                    <Button Background="Transparent" BorderThickness="0" Padding="4" FontSize="12"
                            Foreground="#4169E1" Content="?"
                            ToolTip.Tip="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1007}" />
                  </StackPanel>
                  <TextBlock FontSize="11" Foreground="#8A8F98" TextWrapping="Wrap" Text="{Binding MatchHint}" />
                </StackPanel>

                <!-- 步骤 3: 行为勾选 (勾选顺序 = 菜单键位; 最多 9) -->
                <StackPanel Spacing="4">
                  <TextBlock FontSize="12" Foreground="#8A8F98"
                             Text="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1011}" />
                  <ScrollViewer MaxHeight="260" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding BehaviorPicks}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:BehaviorPickVm">
                          <CheckBox IsChecked="{Binding IsChecked}" IsEnabled="{Binding IsEnabled}"
                                    Padding="4,3" Margin="0,0,0,2">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                              <Border Background="#E8F0FE" CornerRadius="5" Padding="5,1"
                                      IsVisible="{Binding IsChecked}">
                                <TextBlock Text="{Binding Order}" Foreground="#4169E1" FontSize="11"
                                           FontWeight="SemiBold" />
                              </Border>
                              <TextBlock Text="{Binding Label}" FontSize="13" />
                            </StackPanel>
                          </CheckBox>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </ScrollViewer>
                  <!-- 评审 D2: 链接式「管理行为…」入口, 复用视图 OnManageBehaviors
                       (打开 BehaviorLibraryWindow, 关闭后 vm.ReloadBehaviorCatalogAsync 刷新勾选列表) -->
                  <Button Background="Transparent" BorderThickness="0" Padding="0" Margin="0,2,0,0"
                          FontSize="12" Foreground="#4169E1" HorizontalAlignment="Left"
                          Click="OnManageBehaviors"
                          Content="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1083}" />
                </StackPanel>

                <!-- 底部按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" Margin="0,4,0,0">
                  <Button Command="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).CloseAddPanelCommand}"
                          Background="White" BorderThickness="1" BorderBrush="#E3E6EC"
                          CornerRadius="8" Padding="18,7"
                          Content="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=970}" />
                  <Button Command="{Binding ConfirmCommand}" IsEnabled="{Binding CanConfirm}"
                          Background="#4169E1" Foreground="White" CornerRadius="8" Padding="18,7"
                          Content="{Binding $parent[views:SelectedActionPageView].((vm:SelectedActionPageViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=1114}" />
                </StackPanel>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ContentControl.DataTemplates>
      </ContentControl>
    </Border>

  </Grid>
</UserControl>
