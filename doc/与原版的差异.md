# 与原版的差异（开发者版）

> 本文档为**开发者维护用**：逐条记录本 fork 相对上游 [xianyukang/MyKeymap](https://github.com/xianyukang/MyKeymap) 的全部修改（涉及文件、修改原因、技术细节），按日期倒序排列。
> 面向普通用户的简化说明见 [readme.md](../readme.md) 的「与原版的差异」一节。

## 维护规则

> 📌 **维护规则**：新增修改时，将新条目插入到列表**最上方**（倒序排列），格式参照现有条目：文件路径 + 修改内容 + 原因 + 效果。

### 2026-08-31

1. **修复「保存设置后 MyKeymap 退出」**（`config-server/cmd/settings/main.go` / `config-ui-avalonia/Services/BackendSession.cs`）
   - 修改：
     - Go 端 `execCmd`：以 `CREATE_BREAKAWAY_FROM_JOB` (0x01000000) 启动子进程；失败时调 `fallbackExecCmd` 降级——无参数调用（保存设置后的托盘重启）改经 `explorer.exe` 中转启动（explorer 不在本进程 Job 层级内，目标进程彻底脱离任何 Job，代价是不继承提权状态，由 MyKeymap 启动器自行 RunAs）；带参数调用（WindowSpy/GenerateShortcuts 等短暂工具进程）保持普通启动
     - C# 端 `BackendSession`：Job `LimitFlags` 追加 `JOB_OBJECT_LIMIT_BREAKAWAY_OK` (0x0400) 允许子进程脱离；`Shutdown` 由 `Kill(entireProcessTree: true)` 改为 `Kill(false)` 只杀 settings.exe 本体（保存时以 breakaway/explorer 中转重启的 MyKeymap 不再被整树波及）
   - 原因：设置界面 (Avalonia) 将 headless settings.exe 置于 `KILL_ON_JOB_CLOSE` 的 Job 中防孤儿；保存设置时 `SaveConfigHandler` 重启的 MyKeymap 继承了该 Job 成员资格，关闭设置窗口（或界面进程异常退出）时 Job 句柄关闭，托盘被 kernel 连带终止——即「每次保存设置软件就退出」。IsProcessInJob 实验阳性证实（提权环境下保存后新托盘 `inJob=True`）
   - 效果：保存后新托盘 `inOurJob=False`（脱离验证通过），关闭设置窗口不再连带杀托盘；三层防御（breakaway 直启 / Job 开放 BREAKAWAY_OK / explorer 中转兜底）保证在任意 Job 环境（如被自动化测试或安全软件包壳）下均不复发；80/80 测试通过，已构建部署 beta33 并重启实例

### 2026-08-30

1. **移除设置页「帮助页面」选项框及自定义帮助页功能**（`config-ui-avalonia/Views/SettingsPageView.axaml` / `config-ui-avalonia/ViewModels/SettingsPageViewModel.cs` / `config-ui-avalonia/Models/ConfigModels.cs` / `config-ui-avalonia/Services/I18n.cs` / `config-server/cmd/settings/main.go` / `config-server/internal/script/model/types.go` / `config-server/templates/help.html` / `bin/templates/help.html` / `bin/lib/actions/builtins/type8_builtin.ahk`）
   - 修改：UI 删除「📝 帮助页面」分区（折叠按钮 + HTML 编辑区）与 `ShowHelpPage` 分区显隐分支；`Config` 模型（C#/Go 双端）删除 `helpPageHtml` 字段；I18n 删除文案键 903/904/905/906；Go 后端删除保存时生成/清空删除 `bin/site/help.html` 的逻辑与 `saveHelpPageHtml` 函数（`text/template` import 随之移除）；删除 3 处 `help.html` 模板文件（config-server/templates + bin/templates + 部署副本）；AHK 内置函数 `openHelpHtml()` 一并移除（唯一用途是打开 `bin/site/help.html`，功能移除后永不命中）
   - 原因：该功能为 fork 阶段恢复的边缘能力，实际从未使用（部署 config.json 无该字段、无动作引用 openHelpHtml、bin/site/help.html 不存在），移除以减化设置页与维护面
   - 效果：设置页「其他设置」分区减少一项；无用户数据残留风险（已预先核实）；模型与模板零残留引用

### 2026-08-24

1. **文件分组并入文件后缀, 分组表收敛为配置数据**（`config-server/internal/script/actionscheme.go` / `config-server/internal/script/model/types.go` / `config-server/internal/script/config.go` / `config-server/cmd/settings/main.go` / `config-ui/src/components/action/constants.ts` / `config-ui/src/components/action/RuleEditor.vue` / `config-ui/src/components/action/RuleList.vue` / `config-ui/src/types/config.ts` / `bin/lib/rules/SelectedAction.ahk` / `data/config.json`）
   - 修改：
     - **删除「文件分组」匹配类型**：Go 端 `fileGroupExts` 表与 `matchFileGroup` 函数、AHK 端 `MatchFileGroup` 与内置分组表、`MatchActionRule`/`matchActionRule` 的 fileGroup 分支、前端 `MATCH_TYPES` 项与 `FILE_GROUPS` 常量、`RuleEditor` 分组下拉、`RuleList` 分组显示分支、类型注释全部清理；运行时匹配引擎只保留 `fileExt` 后缀列表匹配
     - **分组表入配置**：`config.json` 新增 `fileGroups` 段（`name`/`label`/`exts` 结构，默认 6 组，用户可自行增改——新增「图纸」类分组只需改配置，零代码）；`Config` 模型与前端 `FileGroup` 类型同步；`SaveConfigHandler` 新增 `ValidateFileGroups` 结构校验（名称/显示名/后缀列表非空，非法拒绝保存）
     - **快捷填充 UI**：「文件后缀」条件值输入框下方新增「常用分组快捷填入」下拉，数据源 = 配置 `fileGroups`，选择分组自动展开为逗号分隔后缀列表填入条件值，可继续手改（如「图片但排除 svg」直接删）；配置缺失该段时不显示下拉，降级为纯手输
   - 原因：文件分组与文件后缀匹配逻辑完全同构（fileGroup=image 等价于 fileExt=jpg,jpeg,…），双类型造成用户概念负担与 Go/AHK 双端表同步维护负担；分组表本质是用户可定制的领域数据，符合「数据进配置」原则，且自定义分组可随配置迁移
   - 效果：匹配类型 3 类减为 2 类（fileExt / textType；「默认 (兜底)」已于 2026-09 移除，存量规则不再命中），概念与代码双简；分组定义单一真源在配置，双端内置表同步负担消除；加分组不改代码不重建
   - 取舍（反思）：候选 A 分组表留前端代码（改动最小，但违背数据进配置、自定义分组无法迁移）；候选 B 分组表入配置 + 保存时展开（选定，引擎无分组概念）；候选 C 保留双类型仅改文案（零改动但复杂性与同步负担原样保留）。默认值策略：配置真源写入 6 组默认，Go 不注入默认、前端不内置兑底（fileGroups 缺失是合理降级非坏状态，与 ParseConfig 对 TipSymbol 等「缺失即坏」字段的注入惯例不冲突，且避免默认表双份）

### 2026-08-23

1. **选中动作改造: 移除文本正则 + 特征→行为联动 + 5 类文本特征行为**（`config-server/internal/script/actionscheme.go` / `config-server/internal/script/model/types.go` / `config-server/cmd/settings/actionscheme.go` / `config-server/cmd/settings/main.go` / `config-ui/src/components/action/constants.ts` / `config-ui/src/components/action/RuleEditor.vue` / `config-ui/src/components/action/RuleList.vue` / `config-ui/src/views/SelectedActionEdit.vue` / `config-ui/src/types/config.ts` / `bin/lib/rules/SelectedAction.ahk` / `bin/lib/core/translation.ahk`）
   - 修改：
     - **移除「文本正则」匹配类型**：前端下拉选项（`MATCH_TYPES`）、Go 端匹配分支与 `ValidateActionSchemeRegexes` 预检函数、AHK 端 `MatchActionRule` 分支、类型注释、文档引用全部清理，文本匹配统一走「文本特征」
     - **特征→行为强制联动**：映射单一真源 = Go 端 `textTypeActions`（前端 `TEXT_TYPE_ACTIONS` 与 AHK 端 `ExecuteActionRule` 分支同步副本，注释互指）；前端行为下拉随特征动态渲染（`textType` 时仅展示该特征可选行为），切换特征时若原行为不在新特征可选范围内自动纠正为默认行为；后端 `SaveConfigHandler`（PUT /config）/ `Create/Update /api/action-schemes` / 模拟测试接口三处校验，非法组合返回 400 中文提示（如「文本特征『链接』与行为『程序打开』不匹配, 可选行为: …」）
     - **新增 magnet（磁力链接）文本特征**：`matchTextType`/`MatchTextType` 增加 `^magnet:` 判定，`plain` 判定排除 magnet（纯文本不再吞磁力链接）
     - **新增 5 类文本特征专用行为**（无命令模板，`actionValue` 恒为空，UI 隐藏输入框改信息提示）：`open_url`（默认浏览器打开选中网址，`Run()` 走 ShellExecute 不硬编码浏览器）/ `open_path`（逐行按系统关联程序打开，等同资源管理器双击）/ `open_folder`（选中为目录直接打开，为文件则打开父目录）/ `magnet_download`（先 `RegRead HKCR\magnet\shell\open\command` 检测协议关联，未注册时 Tip 中文提示而非静默失败，再 `Run()` 走 magnet: 协议唤起默认 BT 工具）/ `open_registry`（写 `HKCU\…\Applets\Regedit` 的 `LastKey` 值后重启 regedit 自动定位——系统内置行为，零第三方依赖、无需管理员权限；regedit 已运行时先 `ProcessClose` 再重开）
     - `PreviewAction` 为 5 类新行为返回中文执行预览（模拟测试展示）；规则导入时前端校验组合合法性；`RuleList` 对无参行为直接显示行为名
   - 原因：文本正则与文本特征功能重叠（且 Go RE2 与 AHK PCRE2 语法差异导致测试误报）；特征与行为无约束会出现「选中链接却程序打开」的错配；需求新增 5 类行为，其中磁力下载/注册表定位需可移植实现
   - 效果：配置页不再出现「文本正则」；行为选项随特征动态联动且保存/测试/导入三处校验兜底；5 类行为全部走系统协议/关联程序，磁力下载不依赖具体 BT 软件、注册表定位不依赖 RegJump 等外部工具
   - 方案取舍（反思）：注册表定位候选——LastKey 技巧（零依赖、无需提权，选定）/ RegJump（Sysinternals 官方工具但需捆绑 exe 且普通权限无法执行，否决）/ regedit 地址栏 UI 自动化（跨版本脆弱，否决）；联动候选——前后端同表 + 注释互指（沿用 fileGroupExts 既有惯例，选定）/ 后端元数据 API（单一真源但新增异步复杂度，否决）

### 2026-08-21

1. **winTitle 配置校验引导（settings.exe）**（`config-server/internal/script/action.go`；`config-ui/src/components/actions/ActivateOrRun.vue`；`config-ui/src/store/language-map.ts`）
   - 修改：
     - 前端（settings.exe UI）：winTitle 输入框新增即时校验——裸写 `xxx.exe`（无 `ahk_` 前缀）时红字报错「以 .exe 结尾会被当作窗口标题匹配而永远失败, 应写 ahk_exe 程序名.exe」；label 改为「要激活的窗口 (进程匹配用 ahk_exe 程序名.exe)」；新增常驻 hint 教育文案「窗口标题会变化的软件 (如聊天软件频道页) 不要用标题匹配, 应使用 ahk_exe 进程匹配」（zh/en 双语）
     - 生成层（MyKeymap.exe 与 settings.exe 共用的 `action.go`）：新增 `winTitleWarning` 检测（空/`ahk_`/`ahk-expression:` 前缀放行，大小写不敏感的 `.exe` 结尾命中）→ 生成 MyKeymap.ahk 时在可疑动作前留 `; [配置警告] ...` 注释行
   - 原因：v13 FlClash（裸 `"FlClash.exe"`）与 AyuGram（动态标题）两个唤起 bug 根因均为 winTitle 配置不当，而 settings.exe 对 winTitle 无任何校验引导——用户填什么就是什么，裸 `.exe` 被 AHK 当标题匹配永远失败且无报错（静默走托盘）。生成层注释覆盖所有配置来源（含手改 config.json 的用户），弥补前端校验只覆盖 UI 路径的盲区；不用自动修复（隐式改配置有风险）与保存硬拒绝（覆盖面窄且生硬）
   - 效果：settings.exe UI 输入裸 `.exe` 即时红字拦截；生成的 MyKeymap.ahk 中可疑动作留痕可查（实测：裸 `WindowsTerminal.exe`/大写 `Typora.EXE` 均触发警告，正常 `ahk_exe msedge.exe` 无误报）；beta33 全量配置无警告（已全部规范）；已构建部署 beta33（settings.exe + site 前端产物）并重启实例（26048+25356）

2. **AyuGram 唤起修复（tg 动作，v13 同款模式）**（`beta33/data/config.json` 用户配置）
   - 修改：`tg`（AyuGram）的 `winTitle` 由 `"AyuGram"` 改为 `"ahk_exe AyuGram.exe"`
   - 原因：AyuGram 是 Qt 应用（类 `Qt51519QWindowIcon`），进入频道后窗口标题变为频道名（未读数也动态变化），裸标题 `"AyuGram"` 子串匹配失效 → 可见窗口匹配失败 → 永远走托盘唤起 → 托盘图标 toggle 语义导致「先隐藏再唤起」抖动（日志实证：13:20:00 invoke 后窗口被隐藏 → fallback 可见区单击再显示）；未进频道时标题含 AyuGram，对照测试正常
   - 效果：进频道后最小化到任务栏 → tg 秒激活；可见+活动 → tg 正常最小化；点 X 驻留托盘 → 仍走托盘唤起（Qt 应用不可用 detectHiddenWindow 直显，v11 教训）
   - 诊断实证：AHK 只读匹配——全部窗口隐藏时 `"AyuGram"` 标题匹配 count=0（走托盘根因），`DetectHiddenWindows on` 时 count=5（主窗口 + 3 个 AyuGramDesktop + GDI+ Hook 窗口）；修复后配置生效验证 `ActivateOrRun("ahk_exe AyuGram.exe", "shortcuts\AyuGram.lnk")`，用户实测全场景通过

2. **FlClash/资源管理器唤起修复 v13**（`bin/tray_nav.ps1`；`beta33/data/config.json` 用户配置）
   - 修改：
     - config.json：`fc`（FlClash）的 `winTitle` 由 `"FlClash.exe"` 改为 `"ahk_exe FlClash.exe"`，并加 `"detectHiddenWindow": true`（隐藏窗口直显，见下）；`fe`（文件资源管理器）的 `"explorer.exe"` 同改为 `"ahk_exe explorer.exe"`（同类错误顺带修）
     - `tray_nav.ps1`：可见区兜底（`Find-AndClickIcon`）点击前 `Capture()` 保存鼠标位置（`GetCursorPos` POINT），单/双击验证完成后 `Restore()` 还原——所有 return 路径均还原
   - 原因：`winTitle` 裸写 `"xxx.exe"` 会被 AHK 当作**窗口标题**匹配而永远失败（FlClash 窗口标题实测为 `"FlClash"`，config.json 中 15 个动作仅 fc/fe 两处犯此错误，其余 13 处均正确使用 `ahk_exe` 前缀）→ 失败链：`ActivateWindow` 永不命中（toggle 失效，可见窗口再按不最小化/不激活）→ `WinWait` 失败 → 永远走托盘唤起 → 可见区兜底 `SetCursorPos` 把鼠标移到托盘图标中心且**不还原**（用户实测：唤起后鼠标停在托盘图标上）→ 唤起后 `WinWait` 仍失败不执行 `WinActivate`（窗口保持 FlClash 自身最小化/未激活状态）
   - 效果：可见窗口 toggle 恢复正常（可见+活动 → 最小化到任务栏，再按恢复激活）；隐藏窗口（状态栏驻留）走托盘唤起后 `WinWait` 命中 → 正常激活；鼠标归位（不再停留在托盘图标）；`detectHiddenWindow` 直显**实测通过并保留**：用户真实点 X 隐藏 FlClash（Flutter 应用）后 FC **秒开且可交互**——Flutter 不依赖窗口可见性维护内部状态，外部 `WinShow` 安全，与微信 Qt/QQ Electron 的 v11 卡死形成对比；config.json 修改前备份仍留 %TEMP%\config.json.v13.bak；**验证清单场景 A-E 用户实测全部通过**（A toggle 最小化/恢复、B 最小化激活、C/C' 隐藏直显秒开、D 资源管理器切换、E 微信回归）
   - 附带经验：重启 MyKeymap 时主程序会自动拉起 CommandInput，勿手动启动（否则出现重复实例，需按父进程 PID 清理）

2. **托盘唤起 v12：执行引擎切 pwsh 7**（`bin/lib/core/Functions.ahk`）
   - 修改：`TryTrayRestoreByNav` 的 `RunWait` 命令由 `powershell` 改为 `pwsh`（PS 7+，冷启动 ~260ms vs powershell 5.1 ~600ms）；`try/catch` 回退——无 pwsh 环境时自动改用 `powershell`（脚本本身保持 5.1/7 双兼容）
   - 原因：v11 回退后场景 A 仍 1.1-1.4s，实测分解确认耗时大头是 powershell.exe 5.1 冷启动（565-688ms，v10 基线首次 ~970ms 含系统冷缓存）+ UIAutomation 加载；pwsh 7 冷启动 254-265ms、加载 UIAutomation 后总约 360ms，且 tray_nav.ps1（5.1 兼容语法 + UIAutomationClient/Types 程序集）在 pwsh 7 下完整可用（实测 exit=0、窗口正常弹出）
   - 效果：真机实测（微信场景 A 全链）powershell 1277-1363ms → pwsh 525-571ms（ps1 侧 -58%）；AHK 调用链（TryTrayRestoreByNav + WinWait）797/875ms（原约 1.3s，-40%~-45%）；无 pwsh 的机器自动回退 powershell 行为不变；已部署 beta33（MD5/BOM 校验一致）并重启实例（4344+3316）

2. **托盘唤起极限提速 v11：隐藏窗口直连激活**（`bin/lib/actions/Actions.ahk`）
   - 修改：
     - 新增 `RestoreHiddenWindow`（层 1）：`A_DetectHiddenWindows` 保存/恢复 + `WinGetList("ahk_exe …")` 枚举隐藏窗口（跳过 WS_EX_TOOLWINDOW、消息窗/IME/托盘宿主类）→ 仅对标题含 winTitle 的首个候选调用 `ShowAndActivate`（绝不误弹其他窗口）
     - 新增 `ShowAndActivate`：`ShowWindowAsync(SW_RESTORE=9)` 异步投递 + 轮询 `IsWindowVisible`（最多 300ms/20ms 间隔）——不用 `ShowWindow`/`WinActivate`/`SetForegroundWindow`：同步调用会等待目标窗口响应，微信 Qt/QQ Electron 窗口对激活消息响应慢时会阻塞数秒（探针实证）；SW_RESTORE 自带激活请求
     - `ActivateOrRun` 进程存在分支最前插入层 1：`RestoreHiddenWindow` 成功 → `WinWait(winTitle, , 0.5)` 验证后直接 return（不再 WinActivate）；层 2 兜底链不变（WinWait 0.3 → TryTrayRestoreByNav → 最终兜底 WinWait 2 → Tip），其中 TryTrayRestoreByNav 成功后 `WinWait 1.5 → 0.5`（ps1 v10 已内部轮询验证窗口可见，AHK 侧纯保险）
   - 原因：v10 后场景 A 仍 1.1-1.4s，耗时大头是 powershell.exe 5.1 冷启动约 970ms（引擎固有，UIA 托盘导航本身仅约 300ms）；决策门实测确认微信 4.0（Qt，类 `Qt51514QWindowIcon`）/QQ NT（Electron，类 `Chrome_WidgetWin_1`）关窗驻留 = 主窗口被隐藏（句柄保留）而非销毁 → AHK 进程内 ShowWindowAsync 即可直接恢复，完全绕开 PowerShell 冷启动与 UIA 链路
  - 效果：真机实测（微信/QQ 各 3 轮）场景 A 1102-1444ms → **125ms（-88%~-91%），与场景 B（125-141ms）完全持平**，24 倍差距彻底消除；每应用仅恢复一个可见主窗口（无多弹窗）；层 2 兜底回归 1375ms 完好（tray_nav.ps1 v10 未改）；行为不变（不发 Esc、无开始菜单、不重复启动）；已部署 beta33（MD5/BOM 校验一致）
   - **⚠️ 更正（同日用户实测后回退）**：层 1 方案被证伪——用户真机唤起微信/QQ 后窗口假死（可见但拒绝派发输入）。根因：用户真实点 X 关窗时 Qt/CEF 内部设置托盘模式标志，此后外部 `ShowWindowAsync(SW_RESTORE)` 强改可见性导致应用内部状态不一致（与历史 WinShow 卡死同根因）；v11 验证用 `ShowWindowAsync(SW_HIDE)` 模拟关窗，Qt 内部从未进入托盘模式，因此仅验证了"可见性"而未验证"可交互性"，属无效验证。**已回退**：`Actions.ahk` 恢复 v10 版（git checkout 0c18248，层 1 代码全部移除），重新部署 beta33（MD5 一致）并重启实例，唤起行为恢复 v10 实测状态（1.1-1.4s，功能正确）。

2. **后台程序托盘一键唤出（v9）**（`bin/lib/actions/Actions.ahk` / `bin/lib/core/Functions.ahk` / `bin/lib/core/translation.ahk` / `bin/tray_nav.ps1`）
   - 修改：
     - `Actions.ahk`：`ActivateOrRun` 在「窗口未找到」与「启动程序」之间插入进程存在性检查——`GetTargetProcessName` 解析 exe 名（支持 .lnk 快捷方式）→ 进程存在时 `WinWait(winTitle, , 1.5)` 等窗口出现 → 仍无窗口则 `TryTrayRestoreByNav` 自动唤出 → 失败 `Tip` 提示「程序在后台运行，请点击托盘图标唤出」，**绝不重启新实例**
     - `Functions.ahk`：新增 `GetTargetProcessName`（从 target 解析进程名）；新增 `TryTrayRestoreByNav`（`RunWait` 调 `bin\tray_nav.ps1`，退出码 0=命中激活，替代已证伪的 `TryTrayRestoreByUIA` 并删除 4 个 UIA 辅助函数）；`TrayMenuHandler` 由 switch 改为 if/else 链（2.0.19 解释器无法编译 case 表达式含函数调用/属性访问）
     - `translation.ahk`：新增 `app_running_in_background` 中英文案
     - `bin/tray_nav.ps1`（新增，托盘唤起核心脚本，UTF-8 BOM，PS 托管 UIA）：v9 双路径——① 纯 UIA 展开：`Shell_TrayWnd` 子树 `FindAll(Descendants)` 找 Name="显示隐藏的图标" 的 Button（chevron）→ `InvokePattern.Invoke()` 展开溢出面板（重试 2 次防抖、面板已开则复用），**零键盘模拟**；② 面板匹配：`RootElement` 找类名 `TopLevelWindowForOverflowXamlIsland` → `FindAll(Descendants)` 枚举 Button（Name Trim）→ 两轮精确/包含匹配（排除输入法|指示器|网络|音频|音量|电源|时钟|通知|隐藏）→ `InvokePattern.Invoke()` 激活，激活后**绝不发 Esc**（微信视 Esc 为最小化）；③ 可见区兜底：面板未命中时枚举目标进程所有窗口+uid 0..9，`Shell_NotifyIconGetRect`（NOTIFYICONIDENTIFIER hwnd+uid，物理像素坐标，需 `SetProcessDPIAware`）即时定位 → `SetCursorPos` + `mouse_event` 单击 → `Test-WindowVisible`（MainWindowHandle != 0）验证 → 未弹出再双击（120ms 间隔）→ 1500ms 再验证；④ 日志：追加模式 + 每步时间戳（%TEMP%\mk_traynav.txt）
   - 原因：v7 用 `keybd_event` 模拟 Win+B 展开面板，四次独立调用非原子——Win down 瞬间弹开始菜单、B 被搜索框吞掉，用户实测失败（验证时通过属时序运气）；QQ（Electron，宿主 `Electron_NotifyIconHostWindow`）图标在托盘可见区（UIA 树不可见），单击无效、双击有效
   - 效果：微信/QQ 关窗驻留托盘后按快捷键直接唤出主窗口，无开始菜单闪现、无重复启动；`Actions.ahk`/`Functions.ahk` 已同步部署 beta33；日志全链路可查（`=== run` / `chevron invoked` / `panel appeared` / `MATCH` / `ACTIVATE` / `DONE (window visible)`）

2. **托盘唤起提速 v10（2026-08-21 追加）**（`bin/tray_nav.ps1` / `bin/lib/actions/Actions.ahk`）
   - 修改：
     - `tray_nav.ps1`：① 所有固定等待改 50ms 轮询——chevron Invoke 后面板出现（Wait-Until 650/950ms）、图标 Invoke 后窗口可见（1500ms）、可见区单击/双击后窗口可见（800/1600ms），窗口一出现立即返回，不再空等满额时间；② `TrayNav` C# 互操作类改为延迟编译——仅可见区兜底路径首次进入时才 `Add-Type`（面板主路径零 C# 编译开销）；③ `Get-PanelWindow` 改 `FindFirst` + ClassName 条件直查、`Get-ChevronButton`/面板枚举改 ControlType.Button 条件过滤（减少 UIA 全树枚举）；④ 日志 `=== run` 行提前到 Add-Type 之前（记录冷启动起点）
     - `Actions.ahk`：`ActivateOrRun` 进程存在分支的首次 `WinWait(winTitle, , 1.5)` 缩短为 `0.3`（托盘驻留时窗口不会自行出现，空等无意义；启动中的窗口由 tray_nav.ps1 v10 轮询验证捕获）；`TryTrayRestoreByNav` 成功后拆开单独 `WinWait 1.5`；新增 Tip 前最终兜底 `WinWait(winTitle, , 2)`（程序仍在启动、托盘图标尚未注册导致唤起失败时，窗口出现即激活，消除误报「程序在后台运行」）
   - 原因：基线实测（2026-08-21）场景 A（托盘驻留唤起）约 3.0s vs 场景 B（任务栏最小化激活）约 125ms，差距 24 倍；耗时构成 = powershell 冷启动 + UIAutomation 加载约 970ms + chevron 后固定等待 700ms + 激活后固定等待 1000ms + 其余约 330ms
   - 效果：真机实测（微信/QQ 各 3 次）ps1 全链 1.10-1.44s（较 v9 基线 -52%~-63%），AHK 全链用户感知约 1.5s（原约 4.5s，-67%）；日志显示面板展开 16-32ms、激活到窗口可见 22-87ms（原固定等待 1700ms 已消除）；行为不变（不发 Esc、无开始菜单、不重复启动），调用链回归 result=1，已部署 beta33（MD5/BOM 校验一致）

### 2026-08-19

1. **系统性代码审查与修复**（`config-server/cmd/settings/main.go` / `config-server/internal/matrix/matrix.go` / `config-server/internal/script/config.go` / `bin/lib/core/Functions.ahk` / `bin/lib/actions/Actions.ahk` / `bin/lib/core/KeymapManager.ahk` / `bin/lib/rules/SelectedAction.ahk` / `bin/CollectText.ahk` / `bin/ChangeBrightness.ahk` / `config-ui/src/types/config.ts` / `config-ui/src/views/Settings.vue`）
   - 修改：
     - Go 端：`execCmd` 改用 `cmd.Dir` 指定子进程工作目录，消除全局 `Chdir` 竞态（此前 `os.Chdir("../")` 改变所有 goroutine 的工作目录，与并发读取相对路径配置冲突）；debug 模式 `close(nil)` panic 防护；`rundll32` 硬编码路径改为环境变量解析；代码雨 `randN` 防护 `rand.Intn` 对非正数 panic（极小窗口不再崩溃）；恢复被停用的「自定义帮助页」功能（`HelpPageHtml` 字段 + 保存时生成 `bin/site/help.html`，清空即删除旧文件）；热键分组渲染哨兵值修正（-2233 → -1）
     - AHK 端：`CollectText.ahk` 重启功能修复（`this.__New()` 漏括号导致 GUI 不重建、重复清空收集行）；删除无用 `chromeInstance()`（含硬编码 `C:\Program Files\Google\Chrome` 路径）；`ShowFileInFoler` 拼写修正为 `ShowFileInFolder`；`statie` → `state`；`font-fontFamily` → `font-family`；双引号替换改用 `Chr(34)` 避免字符串连接歧义；删除 KeymapManager 等处注释死代码
     - UI 端：设置页新增「帮助页面 (HTML)」编辑区（`config.helpPageHtml`，保存后生成 `bin/site/help.html`）
   - 原因：审查发现隐藏 bug（并发竞态、close(nil) panic、随机数 panic、失效的重启功能）与无用代码；帮助页功能此前被停用（含配置字段、调用与 UI 入口）
   - 效果：settings.exe 并发稳定性提升、代码雨在极小窗口不再崩溃、帮助页功能恢复可用、代码更易维护；构建产物已同步至 beta33 部署目录（`MyKeymap.ahk` 用户配置以 beta33 版本为准）

2. **打开设置改用 Windows Terminal 承载代码雨**（`bin/lib/core/Functions.ahk`）
   - 修改：`MyKeymapOpenSettings` 由直接 `Run("./bin/settings.exe", "./bin")` 改为通过 Windows Terminal 启动（实际命令 `wt.exe -d "." pwsh.exe -NoExit -Command "& '.\settings.exe'"`，wt 标签以新版 pwsh 7 为 shell 承载 settings.exe 控制台，代码雨显示在 wt 窗口中）；窗口判断由 `WinExist` 改为 `ProcessExist`，进程存在但设置窗口不可见时 `ProcessClose`/`ProcessWaitClose` 后重启
   - 原因：settings.exe 是控制台程序，直接运行弹出的是老式控制台窗口而非 Windows Terminal；改用 wt 承载后无独立窗口标题，原 `WinExist` 判断会失效导致重复启动 settings 进程
   - 效果：打开设置弹出 Windows Terminal 窗口（默认新版 pwsh）显示代码雨，重复点击不会重复启动，关闭 wt 窗口后 settings 随之退出

3. **选中动作模拟测试与匹配一致性修复**（`bin/lib/rules/SelectedAction.ahk` / `config-server/internal/script/actionscheme.go` / `config-server/cmd/settings/actionscheme.go` / `config-server/internal/script/config.go` / `config-ui/src/components/action/ActionTester.vue` / `config-ui/src/components/action/RuleEditor.vue` / `config-ui/src/components/action/constants.ts` / `config-ui/src/views/SelectedActionEdit.vue`）
   - 修改：
     - 扩展名匹配统一忽略大小写：AHK 端 `MatchFileExt`/`MatchFileGroup` 比较改为 `StrLower`，与 Go 端 `EqualFold` 一致（`.JPG` 等大写扩展名也能匹配）
     - 模拟测试接口支持方案快照：`POST /api/action-schemes/test` 新增可选 `scheme` 字段，前端编辑中未保存的修改也能参与测试；无快照时回退读取磁盘配置
     - Go 端 `matchFileExt`/`matchFileGroup` 取后缀后去掉点再比较，与 AHK `SplitPath` 返回不带点扩展名的语义对齐（此前条件值去点后与含点后缀比较，fileExt/fileGroup 规则在测试中永远不匹配）
     - 正则预检：测试接口对 `textRegex` 规则预编译，Go RE2 不支持的语法（如前瞻）返回 400 并提示「AHK 运行时 (PCRE2) 可能支持，请以实际运行为准」，避免误报「不匹配」（⚠️ 2026-08-23 已移除 textRegex 匹配类型，该预检随之删除，文本匹配统一走文本特征）
     - 「默认 (兜底)」规则不在最后一条时显示警告；导入方案时按数组顺序归一化 `priority`；`copyToClipboard` 文案/注释由「执行前」修正为「执行后」；`send_keys` hint 补充 `%selected%` 含按键语法的提醒
   - 原因：AHK 与 Go 两端匹配语义不一致导致测试结果与真实行为偏差；default 规则位置无约束导致其后规则不可达
   - 效果：模拟测试结果与真实运行一致，大小写扩展名/文件分组正确匹配，不支持的语法有明确提示，default 规则位置错误有即时警告

4. **菜单高亮改为显式路由绑定**（`config-ui/src/components/NavigationDrawer.vue`）
   - 修改：「选中动作」与各 keymap 菜单项的 `:active` 改为由 `route.path` 直接判断（选中动作用 `startsWith("/keymap/action")` 同时覆盖列表页与编辑页，keymap 项用精确匹配），不再依赖 vue-router 的 RouterLink 自动匹配
   - 原因：嵌套路由下自动匹配行为不可靠——编辑页 `/keymap/action/:id` 时「选中动作」菜单不高亮；个别环境下路由切换后旧菜单项高亮不熄灭，出现两个菜单项同时高亮
   - 效果：任意路由下左侧菜单恰好只有一项高亮，编辑页也正确高亮「选中动作」

5. **修复设置页三个显示问题**（`config-ui/src/components/NavigationDrawer.vue` / `config-ui/src/components/action/HotkeyCapture.vue`）
   - 修改：
     - `NavigationDrawer.vue`：删除「选中动作」菜单项的 `:active="false"`，恢复路由驱动高亮；侧边栏改弹性布局（虚拟滚动区 `flex-grow-1` 自适应），「保存配置」按钮固定底部可见
     - `HotkeyCapture.vue`：删除快捷键框下方的「^ = Ctrl / ! = Alt / + = Shift / # = Win / < = 左键 / > = 右键」修饰键符号说明（采用监听用户键盘方案，无需符号输入）；`v-text-field` 补 `density="compact"` 与「方案名称」框对齐
   - 原因：菜单选中态不跟随路由（选中动作项永远无法高亮）；快捷键框被说明文字撑高且密度不一致，与相邻输入框不对齐；虚拟滚动固定高度估算不准，底部保存按钮被挤出视口
   - 效果：菜单高亮正确跟随路由切换；快捷键框与方案名称框上下边缘齐平；保存按钮无需滚动即可见

6. **无效热键注册失败不再崩溃**（`config-ui/src/components/action/constants.ts` / `bin/lib/core/KeymapManager.ahk` / `bin/lib/rules/SelectedAction.ahk`）
   - 修改：
     - `constants.ts`：移除反引号键映射（AHK v2 无对应键名），分号键名由 `` `; `` 修正为 `;`
     - `KeymapManager.ahk`：`_Hotkey.Enable()` 中 `Hotkey()` 注册用 try/catch 包裹，失败时 Tip 提示「热键无效, 已跳过」并继续；`Disable()` 对未启用热键直接返回而非断言
     - `SelectedAction.ahk`：`InitActionScheme` 的 `Map()` 加 try/catch 兜底
   - 原因：设置页热键捕获控件曾生成非法 AHK 热键名（如反引号），保存后启动注册时报 `Invalid hotkey` 崩溃退出
   - 效果：单个无效热键方案被自动跳过，其余热键正常注册，脚本不再因坏数据崩溃

7. **新增「选中动作」系统**（`config-server` / `config-ui` / `bin/lib/rules/SelectedAction.ahk`）
   - 修改：
     - 数据层：`config.go` 的 `Config` 新增 `ActionSchemes` 字段与 `ActionScheme`/`ActionRule`/`RuleOptions` 结构体；新增 `internal/script/actionscheme.go`（方案渲染 `actionSchemesCode`、规则匹配 `MatchActionScheme`、执行预览 `PreviewAction`）；`script.go` 注册模板函数
     - API：`cmd/settings/actionscheme.go` 新增 `GET/POST/PUT/DELETE /api/action-schemes[/:id]` 与 `POST /api/action-schemes/test`（模拟测试），`main.go` 注册路由
     - AHK 规则引擎：新增 `bin/lib/rules/SelectedAction.ahk`，`templates/mykeymap.tmpl` 引入并按 `{{ actionSchemesCode .ActionSchemes }}` 渲染；选中文本/文件后按快捷键，依据文件后缀/分组、文本特征逐条匹配规则，执行打开/搜索/命令/按键/脚本/复制六类行为（⚠️ 2026-08-23 文本正则已移除；文本特征新增 magnet 与 5 类专用行为，详见当日记录）
     - UI：`config-ui` 新增 `/keymap/action` 列表页与 `/keymap/action/:id` 编辑页（快捷键捕获、规则拖拽排序、冲突检测、模拟测试、导入导出），类型/store/路由/导航同步更新
   - 原因：参考 RunAny「选中内容 + 快捷键触发预设行为」的能力，为 fork 增加可配置的选中动作系统
   - 效果：可在设置界面可视化配置选中动作方案，规则按优先级匹配（文件后缀 > 文件分组 > 文本特征 > 默认），`%selected%` 变量承载选中内容；未配置 `actionSchemes` 时模板不输出任何代码，行为与原版完全一致

### 2026-08-18

1. **移除关机动作的自动模拟点击**（`bin/lib/actions/Actions.ahk`）
   - 修改：`SystemShutdown()` 删除 `sleep(1300)`、`CoordMode("Mouse", "Screen")`、`MouseClick("Left", 100, 100)` 三行，仅保留 `Run("SlideToShutDown.exe")`
   - 原因：原逻辑在弹出滑动关机界面 1.3 秒后自动模拟点击，导致"界面尚未拖动即自动关机"
   - 效果：呼出滑动关机界面后由用户手动拖动滑块确认，不再自动触发关机
