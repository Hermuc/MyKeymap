name: release

# 打 tag (v*) 才构建并发布正式 Release; workflow_dispatch 用于手动验证构建 (可跳过发布)。
# 产物: MyKeymap-<version>.7z (自包含, 解压即用)。
# 注意: Oracle 对账脚本 (tools/oracle.ps1) 内含本机硬编码路径, 不在 CI 运行;
#       生成契约由 golden 快照测试 + /Validate 覆盖。

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (不含前缀 v, 如 2.0-beta34)'
        required: false
        default: '2.0-beta33-dev'
      skip_release:
        description: '只构建验证, 不发布 Release'
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 40
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Derive version (tag v2.0-beta34 -> 2.0-beta34)
        id: ver
        shell: bash
        run: |
          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: config-server/go.mod
          cache-dependency-path: config-server/go.sum

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Go unit tests
        working-directory: config-server
        run: go test ./...

      - name: Build settings.exe (version injected via ldflags)
        run: |
          go build -C ./config-server -tags=nomsgpack \
            -ldflags "-s -w -X settings/internal/script.MykeymapVersion=${{ steps.ver.outputs.version }}" \
            -o ../bin/settings.exe ./cmd/settings

      - name: Stage headless backend for C# endpoint tests
        shell: bash
        run: |
          mkdir -p "$TEMP/mk_settings_headless"
          cp bin/settings.exe "$TEMP/mk_settings_headless/settings.exe"

      - name: C# unit tests
        run: dotnet test MyKeymap.Settings.Tests/MyKeymap.Settings.Tests.csproj --nologo -v q

      - name: Generate + validate AHK script (/Validate)
        run: |
          bin/settings.exe GenerateAHK ./data/config.json ./config-server/templates/mykeymap.tmpl ./bin/MyKeymap.ahk
          MSYS_NO_PATHCONV=1 bin/AutoHotkey64.exe /ErrorStdOut /Validate ./bin/MyKeymap.ahk

      - name: Sync templates (buildServer 后半段)
        run: |
          rm -rf bin/templates
          cp -r config-server/templates bin/templates

      - name: Publish Avalonia UI (self-contained win-x64 + ReadyToRun)
        shell: pwsh
        run: >
          dotnet publish config-ui-avalonia/MyKeymap.Settings.csproj
          -c Release -r win-x64 --self-contained true
          -p:PublishReadyToRun=true
          -p:SatelliteResourceLanguages="zh-Hans;en"
          -o bin/ui --nologo -v q

      - name: Assert i18n loose resource hash
        shell: bash
        run: cmp config-ui-avalonia/Resources/i18n.json bin/ui/Resources/i18n.json

      - name: Stamp version into sample config
        working-directory: bin
        run: ./settings.exe ChangeVersion "${{ steps.ver.outputs.version }}"

      - name: Assemble release folder
        shell: bash
        run: |
          FOLDER="MyKeymap-${{ steps.ver.outputs.version }}"
          rm -rf "$FOLDER"
          mkdir -p "$FOLDER/shortcuts"
          rm -rf bin/site && cp -r site-assets bin/site
          cp -r data "$FOLDER/"
          cp -r bin "$FOLDER/"
          cp -r tools "$FOLDER/"
          rm -f "$FOLDER/tools/oracle.ps1"
          cp MyKeymap.exe "$FOLDER/"
          cp -- *.bat "$FOLDER/"

      - name: Install 7-Zip
        run: choco install 7zip -y --no-progress

      - name: Pack 7z
        shell: bash
        run: |
          SEVENZIP="$(command -v 7z.exe || echo '/c/Program Files/7-Zip/7z.exe')"
          "$SEVENZIP" a "MyKeymap-${{ steps.ver.outputs.version }}.7z" "MyKeymap-${{ steps.ver.outputs.version }}"

      - name: Upload build artifact (workflow_dispatch 验证用)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/upload-artifact@v4
        with:
          name: MyKeymap-${{ steps.ver.outputs.version }}
          path: MyKeymap-*.7z

      - name: Publish release
        if: ${{ github.event_name == 'push' || inputs.skip_release == false }}
        uses: softprops/action-gh-release@v2
        with:
          files: MyKeymap-*.7z
          generate_release_notes: true
